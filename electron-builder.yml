appId: com.hugh.wongoji-checker
productName: 원고지 첨삭 도우미
directories:
  buildResources: build
  output: dist
files:
  - out/**/*
  - package.json
mac:
  category: public.app-category.education
  target:
    - dmg
    - zip
  artifactName: "${productName}-${version}.${ext}"
